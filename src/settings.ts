export class Settings {

    public static ENABLE_FULL_SCREEN: boolean = false;

    public static TEXT_SPEED: number = 75; // Milliseconds per character
    public static MIN_TEXT_DURATION: number = 1250; // Milliseconds
}